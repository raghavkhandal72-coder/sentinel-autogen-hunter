global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'sentinel-ai-orchestrator'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['ai-orchestrator:8000']
        labels:
          environment: 'production'
          framework: 'autogen-sentinel'
